# 技能书学习系统实现计划

## 一、需求分析

### 1.1 功能需求
- 点击背包中的技能书，可以学习技能
- 学习新技能：获得对应的技能
- 升级技能：将已有技能升级到更高级别
- 学习成功后消耗技能书并显示提示消息

### 1.2 技能书类型
根据 `src/data/skillBooks.ts` 和参考文档分析：

| 技能书类型 | 说明 | 示例 |
|-----------|------|------|
| 学习新技能 | `isUpgrade=false` 或无此字段 | 星魔剑技能书、飞天连斩技能书 |
| 升级技能 | `isUpgrade=true` | 高级风斩、高级星魔剑技能书 |
| 多级升级 | 可升级到5级 | 斗志抑扬系列（1-5级） |

### 1.3 技能系统现状
- ✅ 已有技能数据定义 (`src/data/skillData.ts`)
- ✅ 已有技能书数据 (`src/data/skillBooks.ts`)
- ✅ 已有技能工具函数 (`src/utils/skillUtils.ts`)
- ✅ 已有背包系统和物品使用框架 (`src/App.tsx`)
- ❌ 缺少技能书使用的具体逻辑

## 二、技术方案

### 2.1 核心实现步骤

#### 步骤1：创建技能学习工具函数
**文件**：`src/utils/skillLearnUtils.ts`（新建）

**功能**：
- `learnSkillFromBook()`: 从技能书学习技能的核心逻辑
- `canLearnSkill()`: 检查是否可以学习该技能书
- `getSkillLearnMessage()`: 获取学习成功/失败的提示消息

**核心逻辑**：
```typescript
/**
 * 从技能书学习技能
 * @param skillBook 技能书物品
 * @param currentSkills 当前技能列表
 * @returns 学习结果（是否成功、消息、更新后的技能列表）
 */
export function learnSkillFromBook(
  skillBook: InventoryItem,
  currentSkills: SkillDetail[]
): {
  success: boolean;
  message: string;
  updatedSkills: SkillDetail[];
}
```

#### 步骤2：在App.tsx中添加技能书使用逻辑
**文件**：`src/App.tsx`

**修改位置**：`handleUseItem` 函数（约2967行）

**实现逻辑**：
```typescript
const handleUseItem = useCallback((item: InventoryItem) => {
  // 检查是否是技能书
  if (item.type === 'skillBook') {
    const result = learnSkillFromBook(item, skills);
    if (result.success) {
      setSkills(result.updatedSkills);
      setInteractionLog(prev => [...prev, result.message]);
      // 消耗技能书
      // ... 减少物品数量的逻辑
    } else {
      setInteractionLog(prev => [...prev, result.message]);
    }
    return;
  }
  
  // 原有的其他物品处理逻辑...
}, [skills]);
```

#### 步骤3：修改物品详情弹窗
**文件**：`src/components/inventory/ItemDetailModal.tsx`

**修改内容**：
- 为技能书类型物品添加"学习"按钮
- 技能书应该标记为 `usable: true`（已在 skillBooks.ts 中设置）

#### 步骤4：更新技能书数据
**文件**：`src/data/skillBooks.ts`

**检查项**：
- ✅ 所有技能书已设置 `type: 'skillBook'`
- ✅ 所有技能书已设置 `skillId` 字段
- ✅ 升级类技能书已设置 `isUpgrade: true`
- ✅ 升级类技能书已设置 `targetLevel`
- ⚠️ 需要确保所有技能书设置 `usable: true`

## 三、详细实现

### 3.1 创建 skillLearnUtils.ts

```typescript
/**
 * 技能学习工具函数
 * 处理技能书学习逻辑
 */

import type { InventoryItem, SkillDetail } from '../types';

/**
 * 学习技能的结果
 */
export interface SkillLearnResult {
  success: boolean;           // 是否成功
  message: string;            // 提示消息
  updatedSkills: SkillDetail[]; // 更新后的技能列表
}

/**
 * 从技能书学习技能
 * @param skillBook 技能书物品
 * @param currentSkills 当前技能列表
 * @returns 学习结果
 */
export function learnSkillFromBook(
  skillBook: InventoryItem,
  currentSkills: SkillDetail[]
): SkillLearnResult {
  // 1. 验证是否为技能书
  if (skillBook.type !== 'skillBook') {
    return {
      success: false,
      message: '该物品不是技能书',
      updatedSkills: currentSkills
    };
  }

  // 2. 获取技能ID
  const skillId = skillBook.skillId;
  if (!skillId) {
    return {
      success: false,
      message: '技能书数据错误：缺少技能ID',
      updatedSkills: currentSkills
    };
  }

  // 3. 查找对应技能
  const skillIndex = currentSkills.findIndex(s => s.id === skillId);
  if (skillIndex === -1) {
    return {
      success: false,
      message: '未找到对应技能',
      updatedSkills: currentSkills
    };
  }

  const targetSkill = currentSkills[skillIndex];
  const isUpgrade = skillBook.isUpgrade || false;
  const targetLevel = skillBook.targetLevel || 1;

  // 4. 检查是否可以学习
  if (isUpgrade) {
    // 升级技能书：需要已学习该技能，且当前等级 < 目标等级
    if (!targetSkill.isLearned) {
      return {
        success: false,
        message: `需要先学习 ${targetSkill.name} 技能`,
        updatedSkills: currentSkills
      };
    }
    
    if (targetSkill.level >= targetLevel) {
      return {
        success: false,
        message: `${targetSkill.name} 已达到或超过该等级`,
        updatedSkills: currentSkills
      };
    }
  } else {
    // 学习新技能书：需要未学习该技能
    if (targetSkill.isLearned) {
      return {
        success: false,
        message: `已经学会了 ${targetSkill.name}`,
        updatedSkills: currentSkills
      };
    }
  }

  // 5. 学习/升级技能
  const updatedSkills = [...currentSkills];
  const newSkill = { ...targetSkill };
  
  if (isUpgrade) {
    // 升级技能
    newSkill.level = targetLevel;
    newSkill.isLearned = true;
    updatedSkills[skillIndex] = newSkill;
    
    return {
      success: true,
      message: `恭喜！${targetSkill.name} 升级成功！当前等级：Lv.${targetLevel}`,
      updatedSkills
    };
  } else {
    // 学习新技能
    newSkill.level = targetLevel;
    newSkill.isLearned = true;
    updatedSkills[skillIndex] = newSkill;
    
    return {
      success: true,
      message: `恭喜！学会了新技能：${targetSkill.name}`,
      updatedSkills
    };
  }
}

/**
 * 检查是否可以使用技能书
 * @param skillBook 技能书物品
 * @param currentSkills 当前技能列表
 * @returns 是否可以使用
 */
export function canLearnSkill(
  skillBook: InventoryItem,
  currentSkills: SkillDetail[]
): boolean {
  const result = learnSkillFromBook(skillBook, currentSkills);
  return result.success;
}
```

### 3.2 修改 App.tsx

在 `handleUseItem` 函数中添加技能书处理逻辑：

```typescript
const handleUseItem = useCallback((item: InventoryItem) => {
  // 检查是否是技能书
  if (item.type === 'skillBook') {
    const result = learnSkillFromBook(item, skills);
    
    // 显示结果消息
    setInteractionLog(prev => [...prev, result.message]);
    
    if (result.success) {
      // 更新技能列表
      setSkills(result.updatedSkills);
      
      // 消耗技能书
      setInventory(prev => {
        const index = prev.findIndex(i => i.id === item.id);
        if (index !== -1) {
          const newInventory = [...prev];
          if (newInventory[index].quantity > 1) {
            newInventory[index] = {
              ...newInventory[index],
              quantity: newInventory[index].quantity - 1,
            };
          } else {
            newInventory.splice(index, 1);
          }
          return newInventory;
        }
        return prev;
      });
    }
    return;
  }
  
  // 原有的满经验球处理逻辑
  if (item.name === '满经验球') {
    setCurrentUseItem(item);
    setShowUseItemTargetModal(true);
  } else {
    // 其他可使用物品的处理逻辑
    // ...
  }
}, [skills]);
```

### 3.3 更新技能书数据

在 `src/data/skillBooks.ts` 中为所有技能书添加 `usable: true`：

```typescript
export const skillBookItems: InventoryItem[] = [
  {
    id: 'skillbook_wind_slash_advanced',
    name: '高级风斩',
    icon: '📜',
    type: 'skillBook',
    description: '将风斩升级为高级版本，伤害提升至150%。消耗5点体力。',
    quantity: 1,
    quality: 3,
    price: 1000,
    stackable: false,
    usable: true,  // 添加此字段
    skillId: 'skill_wind_slash',
    isUpgrade: true,
    targetLevel: 2
  },
  // ... 其他技能书同样添加 usable: true
];
```

## 四、测试计划

### 4.1 功能测试

| 测试场景 | 预期结果 |
|---------|---------|
| 使用星魔剑技能书（未学习） | 成功学习星魔剑技能，技能书消失 |
| 使用星魔剑技能书（已学习） | 提示"已经学会了星魔剑"，技能书不消失 |
| 使用高级星魔剑技能书（未学习星魔剑） | 成功学习高级星魔剑（等级2） |
| 使用高级风斩技能书（未学习风斩） | 提示"需要先学习风斩技能" |
| 使用高级风斩技能书（已学习风斩） | 成功升级为高级风斩 |
| 使用斗志抑扬技能书（连续5次） | 依次升级到Lv.1、Lv.2、Lv.3、Lv.4、Lv.5 |
| 使用斗志抑扬技能书（已满级） | 提示"斗志抑扬已达到或超过该等级" |

### 4.2 边界测试

- 背包中没有技能书时，不显示学习按钮
- 技能书数量为0时，自动从背包移除
- 技能书堆叠数量正确减少

## 五、注意事项

### 5.1 数据一致性
- 确保技能书数据中的 `skillId` 与 `skillData.ts` 中的技能ID一致
- 确保技能书的 `targetLevel` 不超过技能的 `maxLevel`

### 5.2 用户体验
- 学习成功后显示明确的提示消息
- 学习失败时保留技能书，不消耗
- 在物品详情弹窗中显示技能书的学习条件

### 5.3 代码规范
- 遵循项目规则：增加注释
- 复用现有函数，避免重复代码
- 保持与现有代码风格一致

## 六、实现文件清单

| 文件路径 | 操作 | 说明 |
|---------|------|------|
| `src/utils/skillLearnUtils.ts` | 新建 | 技能学习工具函数 |
| `src/App.tsx` | 修改 | 添加技能书使用逻辑 |
| `src/data/skillBooks.ts` | 修改 | 添加 `usable: true` 字段 |
| `src/components/inventory/README.md` | 更新 | 更新文档说明技能书学习功能 |

## 七、后续优化建议

1. **技能书详情展示**：在物品详情弹窗中显示技能的详细信息（伤害、消耗等）
2. **学习条件提示**：在物品详情中显示学习该技能书的前置条件
3. **技能预览**：学习前可以预览技能效果
4. **音效反馈**：学习技能时播放音效（参考原版游戏）
5. **技能书商店**：在商店中可以购买技能书
